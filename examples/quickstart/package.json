{
  "name": "evomap-quickstart",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "EvoMap quickstart — OAuth2 + PKCE, the API, and webhook verification over the raw HTTP API (zero SDK)",
  "scripts": {
    "start": "node --env-file=.env index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
