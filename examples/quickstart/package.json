{
  "name": "evomap-quickstart",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "EvoMap quickstart — OAuth2 + PKCE, the API, and webhook verification with @evomap/sdk",
  "scripts": {
    "start": "node --env-file=.env index.js"
  },
  "dependencies": {
    "@evomap/sdk": "file:../../sdk",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
